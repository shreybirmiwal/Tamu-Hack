import React from 'react';

const SeatWithParty = ({ onBack }) => {
  return (
    <div>
      <h2>SeatWithParty Preference</h2>
      {/* Your component content */}
      <button onClick={onBack}>Back</button>
    </div>
  );
};

export default SeatWithParty;
